# Development Scripts

This directory is reserved for small, documented commands that make common development tasks repeatable—for example local setup, starting both applications, regenerating the typed API client, database maintenance, and validation.

Scripts should be cross-platform where practical, safe to run repeatedly, and never contain application business logic.
